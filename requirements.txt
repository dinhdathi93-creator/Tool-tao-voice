# TAO_VOICE - chay CPU, khong can GPU.
#
# Luu y: torch nen cai truoc bang ban CPU cho nhe (CAI_DAT.bat da lo):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

pocket-tts>=2.1.0     # engine doc giong cua Kyutai (100M tham so, chay CPU)
soundfile>=0.12.0     # doc giong mau khong phai WAV 16-bit (mp3, flac, m4a...)
faster-whisper>=1.0.0 # nhan dang moc thoi gian de xuat SRT
numpy>=2
